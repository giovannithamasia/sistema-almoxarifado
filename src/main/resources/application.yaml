spring:
  application:
    name: sistema-almoxarifado
  datasource:
    url: jdbc:mysql://localhost:3306/saep_db
    username: root
    password:
  jpa:
      hibernate:
       ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.MySQLDialect
